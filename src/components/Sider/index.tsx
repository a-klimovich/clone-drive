import { Layout } from 'antd';
import './styles.scss';

const { Sider } = Layout;

const SideMenu: React.FC = () => {
  return (
    <Sider style={{'background': 'transparent'}} className='sider-menu'>
      
    </Sider>
  )
}

export default SideMenu;